# Convex Deployment
CONVEX_DEPLOYMENT=
CONVEX_SITE_URL=

# AI Services (Consider using encrypted secrets in production)
PERPLEXITY_API_KEY=

# Email Service (Not currently used)
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=

# Security Configuration
# Master encryption key for encrypting other secrets (REQUIRED in production)
MASTER_ENCRYPTION_KEY=

# CSRF Protection
CSRF_SECRET=

# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Security Monitoring
SECURITY_WEBHOOK_URL=

# Logging
LOG_LEVEL=info
NODE_ENV=development

# Stripe (if using payments)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
SITE_URL=
# Stripe price IDs per plan/billing (use either these OR products below)
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_YEARLY=
STRIPE_PRICE_MAX_MONTHLY=
STRIPE_PRICE_MAX_YEARLY=
# Stripe product IDs (optionally per billing)
STRIPE_PRODUCT_PRO=
STRIPE_PRODUCT_MAX=
STRIPE_PRODUCT_PRO_MONTHLY=
STRIPE_PRODUCT_PRO_YEARLY=
STRIPE_PRODUCT_MAX_MONTHLY=
STRIPE_PRODUCT_MAX_YEARLY=

# AWS Configuration (if using AWS services for key management)
# AWS_REGION=us-east-1
# AWS_ROLE_ARN=
# AWS_KMS_KEY_ID=

# Example of encrypted secret (encrypted using MASTER_ENCRYPTION_KEY):
# ENCRYPTED_API_KEY=iv:authtag:ciphertext
