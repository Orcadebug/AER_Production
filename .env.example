# Convex Deployment
CONVEX_DEPLOYMENT=
CONVEX_SITE_URL=

# AI Services (Consider using encrypted secrets in production)
PERPLEXITY_API_KEY=

# Email Service (Not currently used)
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=

# Google OAuth (Optional)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Security Configuration
# Master encryption key for encrypting other secrets (REQUIRED in production)
MASTER_ENCRYPTION_KEY=

# CSRF Protection
CSRF_SECRET=

# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Security Monitoring
SECURITY_WEBHOOK_URL=

# Logging
LOG_LEVEL=info
NODE_ENV=development

# Stripe (if using payments)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# AWS Configuration (if using AWS services for key management)
# AWS_REGION=us-east-1
# AWS_ROLE_ARN=
# AWS_KMS_KEY_ID=

# Example of encrypted secret (encrypted using MASTER_ENCRYPTION_KEY):
# ENCRYPTED_API_KEY=iv:authtag:ciphertext
